# Progress

## Current Phase
MVP 首版已实现并完成人工验收。

## Done
- 明确产品目标为 `macOS 菜单栏应用`
- 确认切换对象为 `Codex CLI 本地认证`
- 确认 MVP 只做本地账号切换与 `5h / 周` 剩余额度展示
- 验证本地认证源为 `~/.codex/auth.json` 与 `~/.codex/accounts/registry.json`
- 验证额度可从 `~/.codex/sessions/*.jsonl` 的 `token_count.rate_limits` 读取
- 完成 SwiftUI 菜单栏应用骨架
- 完成本地账号切换、额度读取、手动刷新
- 完成最小单元测试并通过 `5` 个测试
- 完成每 5 分钟自动刷新与完整日期时间展示
- 完成真实桌面人工验证
- 完成“登录新账号”入口与 `codex login` 拉起
- 回归测试通过 `8` 个测试
- 完成登录流程取消能力
- 回归测试通过 `9` 个测试
- 完成取消登录残留进程修复
- 回归测试通过 `10` 个测试
- 完成取消登录残留进程问题的人工验证
- 完成按账号关联额度快照
- 回归测试通过 `13` 个测试
- 修复登录完成后“正在刷新账号列表”状态未回收问题
- 回归测试通过 `14` 个测试
- 修复登录成功后新账号因注册表落盘延迟而未及时显示的问题
- 回归测试通过 `15` 个测试
- 修复读取 `registry.json` 命中原子替换空窗时直接失败的问题
- 回归测试通过 `16` 个测试
- 修复 `registry.json.last_usage` 原生结构兼容性问题，避免账号读取时报 `The data couldn’t be read because it is missing.`
- 回归测试通过 `17` 个测试

## In Progress
- 真实桌面复测登录新账号流程

## Next
- 验证登录新账号后提示是否正常消失
- 验证新账号是否会在短时间内自动出现在菜单栏列表
- 验证登录后不再出现 `The data couldn’t be read because it is missing.`

## Notes
当前优先级是做出稳定可用的 MVP，不扩展复杂设置和自动化策略。
本轮额度方案优先保证“账号有历史快照可回显”，不追求切号后立即拿到目标账号实时新额度。
