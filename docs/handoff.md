# Handoff

## Current State
已明确需求：开发 `macOS 菜单栏应用`，管理 `Codex CLI 本地认证`，支持多账号切换，并显示每个账号的 `5h / 周` 剩余额度。
MVP 已完成首版实现，并通过主代理代码审查、自动化测试与人工验证。
首轮体验修正已完成：应用存活期间每 5 分钟自动刷新一次本地状态，`5h / 周` 重置时间已显示完整日期时间。
仓库清理与远端推送已完成。
“登录新账号”入口已完成：菜单栏可调起本机 `codex login`，登录进程结束后会自动刷新本地账号列表。
登录流程卡住问题已修复：若浏览器登录未完成，用户现在可以在菜单栏里主动取消登录并恢复操作。
取消登录后的残留进程问题已修复：取消时会先尝试正常终止，短暂等待后若进程仍存活，再升级为强制 kill 当前 `codex login` 主进程。
本轮人工测试已确认上述登录取消流程可用。

## Current Rules
- 只做 MVP
- 不增加不必要实体，不过度设计，不过度兜底
- 优先复用 `~/.codex/auth.json` 与 `~/.codex/accounts/registry.json`
- `5h / 周` 额度优先从 `~/.codex/sessions/*.jsonl` 的最新 `token_count.rate_limits` 读取
- 切换结果以新开的 Codex CLI 会话生效为准

## Main Risks
- 已运行的 Codex CLI 进程可能不会立即感知账号切换
- 本地会话限额快照可能滞后于刚切换的账号
- `auth.json` 与 `registry.json` 存在并发写风险，需要原子写入
- 菜单栏 UI 已编译通过，但本轮未做真实桌面点击验收
- `codex login` 是外部交互流程，菜单栏应用只能负责拉起和轮询刷新，无法强控浏览器完成状态
- `codex login` 进程可能长时间挂起，需要可取消的退出路径
- `terminate()` 对挂起中的 `codex login` 不一定足够，可能需要升级到更强的终止策略

## Next Step
如需继续，下一步评估按账号缓存各自额度快照。
