# About

`Codex Auth macOS Bar` 是一个轻量的 `macOS` 菜单栏应用，用来管理本机的 `Codex CLI` 多账号环境。

项目目标很直接：

- 在菜单栏里看见当前账号
- 快速切换本地账号
- 查看当前账号实时用量
- 给未激活账号保留历史信息
- 在账号失效时明确提示，而不是静默回退

它只依赖本机已有的 `~/.codex` 数据，不引入新后端，也不追求复杂配置。设计边界也很明确：先把“本地多账号切换 + 用量可见性”做好，再考虑其他能力。
